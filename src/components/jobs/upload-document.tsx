import { Maybe } from "@/types";

interface Props {
  file: Maybe<File>;
}

const UploadDocument = ({}: Props) => {
  return (
    <div>
      <div></div>
    </div>
  );
};

export default UploadDocument;
